import { moduleFor, test } from 'ember-qunit';

moduleFor('service:error-trapper', 'Unit | Service | error trapper', {});

test('it exists', function(assert) {
  let service = this.subject();
  assert.ok(service);
});
